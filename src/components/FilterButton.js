import { Image, TouchableOpacity } from "react-native";
import Filter from "../../assets/Properties/Filter.png";

const FilterButton = () => {
  return <Image className="mx-3 basis-1/8" source={Filter} />;
};
export default FilterButton;
